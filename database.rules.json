{
  "rules": {
    "testHistory": {
      "$userId": {
        ".read": true,
        ".write": true,
        ".indexOn": ["timestamp"]
      }
    },
    "earData": {
      "$userId": {
        ".read": true,
        ".write": true,
        ".indexOn": ["timestamp", "testId"]
      }
    }
  }
}

